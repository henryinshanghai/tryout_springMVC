启示：
    1 可以使用 WebMvcConfigurerAdapter 类型的API 来 对MVC的行为进行配置；
    2 WebMvcConfigurerAdapter类型 是 WebMvcConfigurer的实现类型 - 所以 WebMvcConfigurer的API方法
    也可以用来配置MVC。

    具体有哪些可以配置的东西，参考：
        WebMvcConfigurerAdapter.java
        WebMvcConfigurer.java


